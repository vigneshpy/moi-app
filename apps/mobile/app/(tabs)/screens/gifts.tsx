import React from "react";
import { View } from "react-native";

// Hidden from tabs (href: null in _layout). File exists only because
// expo-router requires a component for every route in the directory.
export default function GiftsStub() {
	return <View />;
}
